#!/usr/bin/env python
"""
Controller for various ports operations
"""

from ports import ports
ports['ftp/wget']

for name, port in ports.iteritems():
  print name, port._depends

from sys import exit
exit()